<%= form_for(@coureur) do |f| %>
  <% if @coureur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coureur.errors.count, "error") %> prohibited this coureur from being saved:</h2>

      <ul>
      <% @coureur.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pseudo %><br>
    <%= f.text_field :pseudo %>
  </div>
  <div class="field">
    <%= f.label :ville %><br>
    <%= f.text_field :ville %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
